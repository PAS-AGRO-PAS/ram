% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_resources.R
\name{define_resources}
\alias{define_resources}
\title{Define Resource Constraints}
\usage{
define_resources(resources, availability, direction)
}
\arguments{
\item{resources}{Character vector of constraint/resource names (e.g., "land", "labor", "protein_min").}

\item{availability}{Numeric vector of right-hand sides (amounts available or required).}

\item{direction}{Character vector of constraint directions; each value must be either "<=" or ">=".}
}
\value{
Data frame with columns: \code{resource}, \code{availability}, and \code{direction}.
}
\description{
Creates a data frame of resource constraints for the RAM model,
allowing both upper (\verb{"<=``) and lower (}">="`) bounds.
}
\examples{
# Upper bounds only
define_resources(
  resources = c("land", "labor"),
  availability = c(100, 200),
  direction = c("<=", "<=")
)

# Mixed upper/lower bounds (min requirement for protein, max for feed)
define_resources(
  resources = c("protein_min", "feed_max"),
  availability = c(2.5, 10),
  direction = c(">=", "<=")
)
}
